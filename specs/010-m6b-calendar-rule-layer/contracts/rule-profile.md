# Contract — Rule Profile

`LegalRuleProfile` is selected only by `profile_id`, exact version and an explicit human confirmation. It must contain a citation, jurisdiction, validity interval, required facts, exclusions, official source URL/hash and review status. Missing/expired/revoked/conflicting profiles cannot be applied. No implicit fallback profile exists.
